generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  binaryTargets   = "native"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
